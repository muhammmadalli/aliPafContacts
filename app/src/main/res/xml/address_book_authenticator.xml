<?xml version="1.0" encoding="utf-8"?>
<!--
    Defines the address-book account type (one per synced CardDAV address book).
    accountType must match AccountConfig.ACCOUNT_TYPE_ADDRESS_BOOK exactly.
-->
<account-authenticator xmlns:android="http://schemas.android.com/apk/res/android"
    android:accountType="ali.paf.contacts.address_book"
    android:icon="@mipmap/ic_launcher"
    android:smallIcon="@mipmap/ic_launcher"
    android:label="@string/account_type_address_book" />
